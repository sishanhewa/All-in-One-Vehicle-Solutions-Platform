info:
  name: 4.2 Get My Packages 🔒
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/inspection/packages/my-packages"
  headers:
    - name: Authorization
      value: Bearer {{company_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
