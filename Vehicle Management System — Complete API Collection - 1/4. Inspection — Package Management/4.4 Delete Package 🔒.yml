info:
  name: 4.4 Delete Package 🔒
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/api/inspection/packages/{{package_id}}"
  headers:
    - name: Authorization
      value: Bearer {{company_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
