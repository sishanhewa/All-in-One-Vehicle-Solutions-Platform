info:
  name: 3.4 Browse Companies (search)
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/inspection/companies?search=Auto"
  params:
    - name: search
      value: Auto
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
