info:
  name: 3.5 Get Company by ID (with packages)
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/inspection/companies/{{company_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
