info:
  name: 1.4 Get User Profile
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/auth/profile"
  headers:
    - name: Authorization
      value: Bearer {{user_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
