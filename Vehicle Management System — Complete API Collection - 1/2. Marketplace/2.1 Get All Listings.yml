info:
  name: 2.1 Get All Listings
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/marketplace"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
