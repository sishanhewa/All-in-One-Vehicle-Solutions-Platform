info:
  name: 2.3 Get Single Listing
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/marketplace/{{listing_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
