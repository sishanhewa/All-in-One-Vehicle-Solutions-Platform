info:
  name: 3.2 Browse All Companies
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/inspection/companies"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
